




--------------------------------------------------------------------------------------------------
Ubuntu系统重启/etc/profile环境变量不生效

~/.profile
# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi




export JAVA_HOME=/opt/java/jdk-11.0.9
export PATH=$JAVA_HOME/bin:$PATH

#export SCALA_HOME=/usr/share/scala-2.11
#export PATH=$SCALA_HOME/bin:$PATH

#export HADOOP_HOME=/opt/hadoop
#export PATH=$HADOOP_HOME/bin:$PATH


--------------------------------------------------------------------------------------------------

ubuntu pro

hangcchhn@163.com

pro attach C1V3QbcGdqMPsWaacx1tNPiA1kGed


--------------------------------------------------------------------------------------------------


网络配置

/etc/network/interfaces
iface ens33 inet staticll
address 192.168.10.163
netmask 255.255.255.0
gateway 192.168.10.2

ifconfig ens33 192.168.10.178 netmask 255.255.255.0

route add default gw 192.168.10.2


--------------------------------------------------------------------------------------------------

ubuntu 20.04
service network-manager stop
rm /var/lib/NetworkManager/NetworkManager.state
service network-manager start
vim /etc/NetworkManager/NetworkManager.conf

[ifupdown]
managed=true

service network-manager restart
--------------------------------------------------------------------------------------------------


ubuntu <= 18.04
apt install unity-tweak-tool

ubuntu >= 20.04
apt install gnome-tweaks




--------------------------------------------------------------------------------------------------


cd /etc/apt/sources.list.d/


apt update:更新安装列表
apt upgrade:升级软件

apt install xxx:安装软件
apt remove --purge xxx:卸载软件及其配置
apt autoremove xxx:卸载软件及其依赖的安装包

--------------------------------------------------------------------------------------------------


apt-get update
apt-get upgrade

apt-get install xxx
apt-get remove xxx
apt-get purge xxx

apt-cache search xxx
apt-cache show xxx
apt-cache depends xxx

--------------------------------------------------------------------------------------------------


dpkg --list|-l:显示已安装的软件
dpkg --show|-s:显示软件详细信息
dpkg --install|-i  安装软件
dpkg --remove|-r  卸载软件
dpkg --purge|-P  卸载软件并删除配置
dpkg --search|-S

--------------------------------------------------------------------------------------------------



apt install net-tools
apt install netstat

apt install vim
apt install gcc
apt install g++

apt install openssh-server


--------------------------------------------------------------------------------------------------


apt install fonts-wqy-microhei

apt install nautilus-open-terminal

apt install ibus-libpinyin




--------------------------------------------------------------------------------------------------
ubuntu 22.04

AppImages require FUSE to run.

sudo add-apt-repository universe
sudo apt install libfuse2


--------------------------------------------------------------------------------------------------



service --status-all



